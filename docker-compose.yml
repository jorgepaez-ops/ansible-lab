version: "3.9"

services:
  node1:
    build: .
    container_name: node1
    hostname: node1
    ports:
      - "2221:22"
    networks:
      - ansible-net

  node2:
    build: .
    container_name: node2
    hostname: node2
    ports:
      - "2222:22"
    networks:
      - ansible-net

  node3:
    build: .
    container_name: node3
    hostname: node3
    ports:
      - "2223:22"
    networks:
      - ansible-net

networks:
  ansible-net:
    driver: bridge
